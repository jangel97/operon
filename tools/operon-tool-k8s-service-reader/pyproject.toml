[project]
name = "operon-tool-k8s-service-reader"
version = "0.1.0"
description = "Operon tool — read Kubernetes service details"
requires-python = ">=3.11"
dependencies = [
    "operon>=0.1.0",
    "operon-tool-k8s-base>=0.1.0",
]

[project.entry-points."operon.tools"]
k8s-service-reader = "operon_tool_k8s_service_reader:K8sServiceReaderTool"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
