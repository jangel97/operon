[project]
name = "operon-tool-k8s-pod-reader"
version = "0.1.0"
description = "Operon tool — read Kubernetes pod status and details"
requires-python = ">=3.11"
dependencies = [
    "operon>=0.1.0",
    "operon-tool-k8s-base>=0.1.0",
]

[project.entry-points."operon.tools"]
k8s-pod-reader = "operon_tool_k8s_pod_reader:K8sPodReaderTool"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
