[project]
name = "operon-tool-k8s-base"
version = "0.1.0"
description = "Shared base for Operon k8s tools — kubectl subprocess wrapper"
requires-python = ">=3.11"
dependencies = [
    "operon>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
